apiVersion: v1
kind: Service
metadata:
  name: waza-metrics
  labels:
    app: waza-metrics
spec:
  selector:
    app: waza-metrics
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
